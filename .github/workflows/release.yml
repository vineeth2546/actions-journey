name: release
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - id: lint
        run: echo "Lint Step"
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - id: test
        run: echo "Test Step"
  package:
    runs-on: ubuntu-latest
    needs: [lint, unit-tests]
    steps:
      - id: pack
        run: echo "Package Step"


